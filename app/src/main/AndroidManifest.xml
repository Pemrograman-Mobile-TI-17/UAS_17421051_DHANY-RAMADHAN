<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kios_buah">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pembeli.HomePembeliActivity"></activity>
        <activity android:name=".pembeli.DetailPembeli" />
        <activity android:name=".admin.Profile" />
        <activity android:name=".admin.InputDataBuah" />
        <activity android:name=".admin.HomeAdminActivity" />
        <activity android:name=".admin.EditBuahDanHapusActivity" />
        <activity android:name=".admin.ActivityDataBuah" />
        <activity android:name=".user.RegistrasiActivity" />
        <activity android:name=".user.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>